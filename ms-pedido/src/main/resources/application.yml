
server:
  port: 8092

spring:
  application:
    name: ms-pedido
    cloud:
    compatibility:
      enabled: false  # Habilita compatibilidad con versiones anteriores de Spring Cloud
  config:
    import: optional:configserver:http://localhost:8888  # URL del servidor de configuración

  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@www.portalacademico.cl:49161/xe
    username: apex_us_dsy_08_gdl
    password: macarena

  jpa:
    hibernate:
      ddl-auto: update  # ⚠️ Solo para desarrollo. Cambiar a 'none' o usar Flyway/Liquibase en producción
    database: oracle
    database-platform: org.hibernate.dialect.OracleDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # Muestra valores reales de parámetros en SQL

eureka:
  instance:
    hostname: localhost
    leaseRenewalIntervalInSeconds: 10
    leaseExpirationDurationInSeconds: 30
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka